/// <reference types="vite/client" />

// Injected by vite.config.ts's `define` — the local proxy path ("/api")
// in dev, or the deployed Cloudflare Worker's URL for GitHub Pages builds.
declare const __API_BASE__: string;
