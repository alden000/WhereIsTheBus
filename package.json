{
  "name": "whereisthebus",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "concurrently -k -n vite,api \"vite\" \"npm run dev --prefix server\"",
    "build": "tsc && vite build",
    "preview": "concurrently -k -n vite,api \"vite preview\" \"npm run start --prefix server\""
  },
  "devDependencies": {
    "@types/leaflet": "^1.9.22",
    "concurrently": "^9.1.0",
    "typescript": "~6.0.2",
    "vite": "^8.2.2"
  },
  "dependencies": {
    "leaflet": "^1.9.4"
  }
}
