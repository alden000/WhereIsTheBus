# Optional. Only needed if you're exposing your local dev server through a
# tunnel (ngrok, Cloudflare Tunnel, etc.) — Vite's dev server otherwise
# rejects requests with a Host header it doesn't recognize.
# Copy this to .env.local (gitignored) and fill in your tunnel's hostname.
TUNNEL_HOST=your-tunnel-hostname.example.com
